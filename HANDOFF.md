# Session Handoff

- The project is undergoing a massive TypeScript to Go architecture shift.
- Core proxying components are built in `internal/combo`, `internal/auth`, and `internal/providers`.
- `mcp-server-go/` has been initialized to handle the new Go native MCP tools architecture.
- All branches and submodules (`CLIProxyAPIPlus`) are clean and fully synced with `main`.
