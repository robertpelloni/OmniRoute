# TODO
- Port configuration and auth mechanisms from CLIProxyAPIPlus to internal/config and internal/auth.
- Map the core handlers from `open-sse` and proxy logic into Go HTTP endpoints.
- Continue expanding tooltips across other dashboard settings such as Providers and Quotas.
- Implement proper DB interfaces using Go database/sql and port SQLite schema.
